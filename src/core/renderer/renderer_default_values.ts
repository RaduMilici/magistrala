import { size } from 'pulsar-pathfinding';

export const DEFAULT_RENDERER_SIZE: size = { width: 500, height: 500 };
export const DEFAULT_RENDERER_CONTAINER: HTMLElement = document.body;
export const DEFAULT_RENDERER_DEPTH: number = 100;
